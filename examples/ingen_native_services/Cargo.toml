[package]
name = "ingen-native-services"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
scan = { path = "../..", features = ["cuda", "wgpu"] }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
